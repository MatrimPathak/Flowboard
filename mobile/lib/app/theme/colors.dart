import 'package:flutter/material.dart';

class FlowboardColors {
  // Brand
  static const primary = Color(0xFF6366F1);    // indigo-500
  static const primaryDark = Color(0xFF4F46E5); // indigo-600

  // Status colors
  static const backlog = Color(0xFF94A3B8);   // slate-400
  static const todo = Color(0xFF64748B);       // slate-500
  static const inProgress = Color(0xFF3B82F6); // blue-500
  static const underReview = Color(0xFFF59E0B); // amber-500
  static const done = Color(0xFF22C55E);        // green-500

  // Priority colors
  static const critical = Color(0xFFEF4444);   // red-500
  static const high = Color(0xFFF97316);        // orange-500
  static const medium = Color(0xFFF59E0B);      // amber-500
  static const low = Color(0xFF22C55E);         // green-500

  // Issue type colors
  static const epic = Color(0xFF8B5CF6);        // violet-500
  static const story = Color(0xFF3B82F6);        // blue-500
  static const bug = Color(0xFFEF4444);          // red-500
  static const spike = Color(0xFFF59E0B);        // amber-500
  static const task = Color(0xFF6366F1);         // indigo-500

  // Surfaces
  static const surfaceLight = Color(0xFFF8FAFC);
  static const surfaceDark = Color(0xFF0F172A);
  static const cardLight = Color(0xFFFFFFFF);
  static const cardDark = Color(0xFF1E293B);

  // Borders
  static const borderLight = Color(0xFFE2E8F0);
  static const borderDark = Color(0xFF334155);
}
